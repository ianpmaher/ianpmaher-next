import IconCard from "./IconCard";

const Projects = (props) => {
    return (
        <div>
            <IconCard variant="projects">
                <h2>stuff</h2>
            </IconCard>
            <h1>Projects</h1>
        </div>
    );
};

export default Projects;
