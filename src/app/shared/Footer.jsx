import Contact from "../components/Contact";

const Footer = (props) => {
    return (
        <footer className={props.className}>
            <Contact />
        </footer>
    );
}

export default Footer;